# Compaction Log - 14032026222613

## Cumulative History

### Prior Progress (Condensed)
- Portfolio/blog site completed with iterative redesigns and JSON-driven content.
- Tailwind migrated from CDN runtime to local Tailwind CLI pipeline (`css/input.css` -> `css/output.css`).
- Certificates/activities optimized from heavy thumbnail grids to lightweight cards with on-demand popup evidence previews.
- Added `howibuild.html` with copied OpenCode prompt files.
- Blog pages redesigned and demo blog post added.
- GitHub Pages deployment configured and live.

## Session 14-03-2026 22:26 - Hero UX Fixes + Developer Loader

### User requests addressed
- Cipher text animation should type in naturally at initial stage (not render full random-length line instantly).
- Move "Open for Opportunities" capsule beside the top hero command capsule and remove it from profile image area.
- Add developer-centric loading animation for page load and image load behavior.

### Changes made in `index.html`
- **Cipher typing fix**
  - Updated `startHeroCipherAnimation()` decrypt phase logic:
    - Now reveals text progressively like typing.
    - Shows one active encrypted character at the typing frontier.
    - Prevents full-width random characters on initial render.
- **Opportunities capsule placement**
  - Removed opportunities badge overlay from profile photo block.
  - Added it beside the `dev@tushar:~$ whoami` capsule in the hero left content row.
- **Page loading animation**
  - Added full-screen developer-style boot loader overlay (`#page-loader`) with terminal-like status lines and spinner.
  - Body now starts in `is-booting` state and unlocks scroll after loader exits.
  - Loader hides after `window.load` (with small delay) and has a fallback timeout.
- **Image loading animation**
  - Added image state classes (`img-pending`, `img-ready`) with blur/fade transition.
  - Added JS image binding utility for existing and dynamically inserted images (via `MutationObserver`).
  - Images now animate until each individual image finishes load/error event.

### Validation
- Reloaded `http://127.0.0.1:5500/index.html`.
- Confirmed:
  - cipher line starts with true typing-like progression,
  - opportunities capsule appears beside whoami capsule,
  - profile image no longer carries opportunities badge,
  - page and image loading animations run without console errors.

---

This file is the latest cumulative memory snapshot. Future sessions should append new progress by creating the next timestamped `.compact_DDMMYYYYHHMMSS.md` with all prior history plus new updates.
